{
  "name": "duet-timeout-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Diagnostic MCP server to measure how long Codex.app / Claude Code (Claude Desktop) will block on a single tool call before timing out, and whether progress notifications extend that window.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "smoketest": "npm run build && node inproc-test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
